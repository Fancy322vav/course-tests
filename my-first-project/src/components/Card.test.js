import "@testing-library/jest-dom";
import Card from "./Card";

import { render, fireEvent, screen } from "./customRender";

describe("Card component", () => {
  test("render card component", () => {});
  render(<Card />);
});
